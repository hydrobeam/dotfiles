\usepackage{xcolor} % define color
\usepackage{hyperref} % links
\usepackage{emoji} % it's in the name
\usepackage{fontspec}
\usepackage{tikz}
\usepackage[margin=1.2in]{geometry} % get the margins right.


% render uncode emoji straight up
% https://tex.stackexchange.com/questions/572212/substituting-fonts-for-emojis-in-lualatex/572220#572220
\directlua{luaotfload.add_fallback
   ("emojifallback",
    {
      "AppleColorEmoji:mode=harf;"
    }
   )}

\setmainfont{texgyretermes-regular}[
  Extension      = .otf ,
  BoldFont       = texgyretermes-bold,
  ItalicFont     = texgyretermes-italic,
  BoldItalicFont = texgyretermes-bolditalic,
  RawFeature={fallback=emojifallback}
]


% for tracing characters that my font doesn't support. not sure how needed this is.

\tracinglostchars=2
\setlength{\parindent}{0pt} % make paragraphs a thing of the past.

% make links blue and pretty.
\hypersetup{colorlinks=true, linkcolor=blue, filecolor=magenta, urlcolor=cyan}

% code setup
\setmonofont[Ligatures=TeX]{Jetbrains Mono}

% emoji setup
\setemojifont{AppleColorEmoji}


\newcommand{\ligne}{\noindent\rule{\textwidth}{0.4pt}} % make a horizontal line
